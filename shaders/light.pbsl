emission(Spectrum power)
{
  Spectrum radiosity = invArea * power;
  E = new HemisphericalEmission(radiosity);
} // end emission()

scattering(void)
{
  F = new Lambertian(Spectrum::white());
} // end scattering()

